<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        
      </button>
      <a class="navbar-brand" href="/">Twitter</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
      <li>
        <% if session[:ex]==1 %>
        <a href="/home/showall">Board</a>
        <% elsif session[:ex]==0 %>
        <a href="/home/read">Post</a>
        <% elsif session[:ex]==4 %>
        <% session.delete(:ex) %>
        <% end %>
      </li>
        <li><% if admin_signed_in? %>
  <%= link_to "로그아웃", destroy_admin_session_path, method: :delete, data: { confirm: "Are you sure?" } %></li>

  <li style="margin-top:16px"><span class="glyphicon glyphicon-user" aria-hidden="true"></span></li>
<% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>